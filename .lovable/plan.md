Remove the contact line `<span>{siteContact.phoneDisplay} · {siteContact.email}</span>` from the custom footer of the `/cabinets-comptables` lead page (`src/pages/CabinetsComptablesPage.tsx`, lines 207-209). The remaining footer will keep only the copyright/address line. Optionally adjust the container so it centers nicely after the removal.